    def testCode(self):
        date = None
        resp = self.controller.mvcm.getalllogs('https://qdlp2bcmapp0002.ess.fiserv.one/mvcm-api/logs')
        #find out how many packages in the returned json
        iteration = from 1 to number of json packages
        resp = self.controller.mvcm.getalllogs(f'https://qdlp2bcmapp0002.ess.fiserv.one/mvcm-api/logs/{iteration}')
        #iterate through each package in the returned json and get the 'name' field, and 'dateModified' field, and 'parent' field
        #if the date is = None then set it to todays date and check for the datemodified field to be the same, if not none then check for when the modified date is 
        # after the specified date, if either is true then create a dictionary where the field's are the 'name' field and then the response.text of this call
        resp = self.controller.mvcm.getalllogs(f'https://qdlp2bcmapp0002.ess.fiserv.one/mvcm-api/logs/download/{iteration}/{parent}/{name}'
        dict of {name} = resp.text
